export class Customer {
  name: string;
  email: string;
  user_id: string;
  constructor(name: string, email: string, user_id: string) {
    this.name = name;
    this.email = email;
    this.user_id = user_id;
  }
}
